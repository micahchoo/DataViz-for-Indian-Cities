deployment:
  basePath: /DataViz-for-Indian-Cities

appearance:
  default: system
  switcher: true

theme:
  colorPalettes:
    default:
      light:
        - "#00dc54" 
        - "#ffe256" 
        - "#8dcbe1" 
        - "#892481"
        - "#cd4063" 
      dark:
        - "#24ff78" 
        - "#a88c00" 
        - "#1e5b71" 
        - "#db76d2"
        - "#bf3154" 
colorScales:
    custom:
      light:
        - "#FAF0FA"  # Lightest (soft periwinkle)
        - "#E2C6FA"  # Light (pale violet)
        - "#D4B5FA"  # Medium-light (soft purple)
        - "#B5D1FA"  # Medium (light sky blue)
        - "#C4DDFA"  # Medium-light blue (pale blue)
        - "#de988a"  # Medium-warm (pastel peach)
        - "#e0a8b5"  # Medium-warm (light rose)
        - "#eba0c0"  # Deep-warm (baby pink)
        - "#8f6b7e"  # Dark (deepest tone)
      dark:
        - "#e8d4f2"  # Lightest (bright periwinkle for contrast)
        - "#d4b8e6"  # Light (bright violet)
        - "#b89ed9"  # Medium-light (medium purple)
        - "#9db9d4"  # Medium (medium blue)
        - "#8eadd4"  # Medium-dark blue
        - "#b5807a"  # Medium-warm (deep peach)
        - "#a67682"  # Deep-warm (deep rose)
        - "#9d6a85"  # Deeper-warm (rich pink)
        - "#6b4d5e"  # Darkest (deepest tone)
colors:
    primary:
      light: "#2563eb"
      dark: "#3b82f6"
    accent:
      light: "#c2410c"
      dark: "#fdba74"
    base:
      light: "#ffffff"
      dark: "#09090b"
    info:
      light: "#0284c7"
      dark: "#38bdf8"
    positive:
      light: "#16a34a"
      dark: "#4ade80"
    warning:
      light: "#f8c900"
      dark: "#fbbf24"
    negative:
      light: "#dc2626"
      dark: "#f87171"

plugins:
  components:
    # This loads all of evidence's core charts and UI components
    # You probably don't want to edit this dependency unless you know what you are doing
    "@evidence-dev/core-components": {}

  datasources:
    # You can add additional datasources here by adding npm packages. 
    # Make to also add them to `package.json`.
    "@evidence-dev/bigquery": { }
    "@evidence-dev/csv": { }
    "@evidence-dev/databricks": { }
    "@evidence-dev/duckdb": { }
    "@evidence-dev/mssql": { }
    "@evidence-dev/mysql": { }
    "@evidence-dev/postgres": { }
    "@evidence-dev/source-javascript": { }
    "@evidence-dev/snowflake": { }
    "@evidence-dev/sqlite": { }
    "@evidence-dev/trino": { }
    "@evidence-dev/motherduck": { }
      # light:
      #   - "${textL}" 
      #   - "${bgL}" 
      #   - "${primaryL}" 
      #   - "${secondaryL}"
      #   - "${accentL}" 
      # dark:
      #   - "${textD}" 
      #   - "${bgD}" 
      #   - "${primaryD}" 
      #   - "${secondaryD}"
      #   - "${accentD}" 
